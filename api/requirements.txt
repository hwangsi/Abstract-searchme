# Web API dependencies (Vercel Python functions).
# Root requirements.txt is the desktop app's and is excluded via .vercelignore.
pymupdf>=1.24.0
rapidfuzz>=3.0
arrow>=1.3
psycopg[binary]>=3.1
tzdata>=2024.1
