/** @type {import('next').NextConfig} */
const nextConfig = {
  // Python functions in api/ are served by Vercel (prod) or `vercel dev` (local).
  // `next dev` alone serves only the front end — use `npm run dev` (vercel dev).
};

export default nextConfig;
